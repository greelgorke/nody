module.exports.events = { EVENT_STATE_CHANGED : 'pipe-state-change'
                        }

module.exports.states = { PIPE_STATE_CLOSED : 'closed'
                        , PIPE_STATE_ERROR  : 'error'
                        }

module.exports.NOP = ''